import { moreComponents } from './more-components.js';

describe('moreComponents', () => {
  it('should work', () => {
    expect(moreComponents()).toEqual('more-components and other things');
  });
});
